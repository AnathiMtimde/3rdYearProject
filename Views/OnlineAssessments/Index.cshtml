@model IEnumerable<DGSappSem2.Models.AssessmentBusiness.OnlineAssessment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Assessments.AssessmentName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Staffs.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Students.StudentName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Document)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Feedback)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Assessments.AssessmentName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Staffs.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Students.StudentName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Document)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Feedback)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OnlineAssessmentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OnlineAssessmentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OnlineAssessmentID })
        </td>
    </tr>
}

</table>
